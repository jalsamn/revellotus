<%= form_for(@productrequest) do |f| %>
  <% if @productrequest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@productrequest.errors.count, "error") %> prohibited this productrequest from being saved:</h2>

      <ul>
      <% @productrequest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label "Product Name" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :phone %> (Optional, if you would like to be notified)<br>
    <%= f.text_field :phone %>
  </div>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.select :location, [['North Phoenix','phx'],['Mesa','mesa']] %>

  </div>
  <div class="actions">
    <%= f.submit "Submit Request" %>
  </div>
<% end %>
