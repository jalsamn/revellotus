<%= form_for(@produce_order_item) do |f| %>
  <% if @produce_order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produce_order_item.errors.count, "error") %> prohibited this produce_order_item from being saved:</h2>

      <ul>
      <% @produce_order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :producename %><br>
    <%= f.text_field :producename %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.text_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
